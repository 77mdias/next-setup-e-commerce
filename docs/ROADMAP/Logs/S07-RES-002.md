# S07-RES-002 - Introduzir reserva atômica de estoque no início do checkout

**Data:** 2026-03-19  
**Status:** ✅ Concluída

## Resumo

O checkout passou a criar reservas de estoque de forma atômica antes de avançar para a sessão Stripe, eliminando a janela de oversell entre validação de saldo e criação do pagamento.

- O pedido, os itens e as reservas agora nascem na mesma transação.
- A mutação do inventário usa incremento atômico de `reserved` com guardas sobre disponibilidade e `minStock`.
- Falhas posteriores no checkout liberam as reservas antes de apagar o pedido órfão.

## Discovery técnico aplicado

- Fluxo anterior:
  - `POST /api/checkout` validava saldo com leitura de `quantity - reserved`, criava pedido e só depois chamava Stripe.
  - Não havia mutação de `reserved` no início do checkout, então duas requisições concorrentes podiam passar pela mesma leitura.
- Contratos entre camadas:
  - `StockReservation` já existia via `S07-RES-001`, mas o endpoint ainda não consumia o módulo.
  - O modo `E2E_CHECKOUT_MOCK_MODE` atualiza status do pedido dentro do próprio checkout e precisava receber tratamento explícito para reservas.
- Riscos mapeados:
  - oversell por corrida no checkout;
  - reserva órfã em falha de Stripe/logo após criação do pedido;
  - divergência entre pedido cancelado no mock E2E e estoque ainda reservado.

## Decisões técnicas

- A reserva atômica foi centralizada em `src/lib/stock-reservation.ts` com `UPDATE "inventory"` condicional e incremento direto de `reserved`.
- A criação do pedido passou a selecionar os `orderItems` persistidos na mesma transação para vincular cada `StockReservation` ao item correto.
- O rollback manteve a estratégia existente de apagar pedido órfão quando Stripe falha, mas agora libera as reservas na mesma transação de compensação.
- No mock E2E:
  - sucesso confirma as reservas vinculadas ao pedido;
  - falha libera as reservas imediatamente.

## Arquivos alterados

- `src/lib/stock-reservation.ts`
- `src/lib/__tests__/stock-reservation.test.ts`
- `src/app/api/checkout/route.ts`
- `src/app/api/checkout/__tests__/route.integration.test.ts`
- `docs/development/tasks/PHASE-07-estoque-fulfillment-confiavel.md`

## Validações executadas

- ✅ `npm run prisma:generate`
- ✅ `npm run typecheck`
- ✅ `npm run lint`
- ✅ `npm run format:check`
- ✅ `npm run build`
- ✅ `npm run test:unit -- src/lib/__tests__/stock-reservation.test.ts`
- ✅ `npm run test:integration -- src/app/api/checkout/__tests__/route.integration.test.ts`
- ✅ `npm run test:integration:critical`
- ⚠️ `npm run test:unit:critical`
  - Falha fora do escopo em `src/components/admin/orders/orders-view.test.ts` por expectativa textual (`"Operacao de pedidos"`) divergente do HTML renderizado.
- ⚠️ `npm run test:e2e:critical:ci`
  - Bloqueado por indisponibilidade do banco Neon durante `scripts/prepare-e2e-data.js`.

## Checklist de aceite da task

| Critério de aceitação                                                                  | Evidência                                                                                           | Status |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------ |
| Checkout com saldo disponível cria reserva consistente antes de avançar para pagamento | `POST /api/checkout` cria pedido + itens + reservas na mesma transação antes de chamar Stripe       | ✅     |
| Compras concorrentes para o último item resultam em no máximo uma reserva bem-sucedida | teste unitário concorrente em `src/lib/__tests__/stock-reservation.test.ts` valida 1 sucesso máximo | ✅     |

## Riscos remanescentes e mitigação

- Risco: reservas confirmadas ainda permanecem contabilizadas em `Inventory.reserved` até a etapa de consumo definitivo do webhook.
  - Mitigação: coberto pela sequência da sprint em `S07-FUL-001`.
- Risco: expiração/liberação de reservas abandonadas ainda depende da task seguinte.
  - Mitigação: coberto em `S07-RES-003`.
- Risco: o E2E crítico segue dependente de conectividade externa com Neon.
  - Mitigação: repetir `npm run test:e2e:critical:ci` quando o banco estiver acessível.
